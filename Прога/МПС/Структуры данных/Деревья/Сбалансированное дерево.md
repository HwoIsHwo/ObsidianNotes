Сбалансированное бинарное дерево — это дерево поиска, у которого высота поддерживается логарифмической относительно числа узлов. Основная цель — гарантировать сложность `O(log N)` для поиска, вставки и удаления независимо от порядка поступления данных.

Если использовать обычное бинарное дерево поиска (BST), оно легко вырождается в список. Высота становится `O(N)`, и все операции деградируют. Сбалансированные деревья вводят **ограничение на разницу высот поддеревьев** и автоматически перестраиваются после модификаций.

Существует несколько семейств:
- AVL-деревья
- красно-чёрные деревья
- B-tree / B+-tree
- Treap
- Splay tree
В STL используется красно-чёрное дерево.


## Принцип балансировки
Идея одинакова: после вставки или удаления вычисляется метрика баланса узла. Если она выходит за допустимые пределы, выполняется **поворот (rotation)** — локальная перестройка структуры без нарушения свойства BST.
Простейший пример — правый поворот:
```
      y                x
     /                / \
    x       ->       A   y
   / \                  / \
  A   B                B   C
         \
          C
```


## Минимальная реализация на C (AVL-дерево)
AVL — самое простое для понимания строго сбалансированное дерево.
Минимальная реализация вставки:
```C
#include <stdio.h>
#include <stdlib.h>

typedef struct Node
{
    int key;
    int height;
    struct Node* left;
    struct Node* right;
} Node;

int height(Node* n)
{
    return n ? n->height : 0;
}

int max(int a, int b)
{
    return a > b ? a : b;
}

Node* create(int key)
{
    Node* n = malloc(sizeof(Node));
    n->key = key;
    n->height = 1;
    n->left = n->right = NULL;
    return n;
}

/* правый поворот */
Node* rotate_right(Node* y)
{
    Node* x = y->left;
    Node* T2 = x->right;

    x->right = y;
    y->left = T2;

    y->height = max(height(y->left), height(y->right)) + 1;
    x->height = max(height(x->left), height(x->right)) + 1;

    return x;
}

/* левый поворот */
Node* rotate_left(Node* x)
{
    Node* y = x->right;
    Node* T2 = y->left;

    y->left = x;
    x->right = T2;

    x->height = max(height(x->left), height(x->right)) + 1;
    y->height = max(height(y->left), height(y->right)) + 1;

    return y;
}

int balance(Node* n)
{
    return n ? height(n->left) - height(n->right) : 0;
}

Node* insert(Node* node, int key)
{
    if (!node)
        return create(key);

    if (key < node->key)
        node->left = insert(node->left, key);
    else if (key > node->key)
        node->right = insert(node->right, key);
    else
        return node;

    node->height =
        1 + max(height(node->left), height(node->right));

    int b = balance(node);

    /* 4 случая разбалансировки */

    if (b > 1 && key < node->left->key)
        return rotate_right(node);

    if (b < -1 && key > node->right->key)
        return rotate_left(node);

    if (b > 1 && key > node->left->key)
    {
        node->left = rotate_left(node->left);
        return rotate_right(node);
    }

    if (b < -1 && key < node->right->key)
    {
        node->right = rotate_right(node->right);
        return rotate_left(node);
    }

    return node;
}

void inorder(Node* root)
{
    if (!root) return;
    inorder(root->left);
    printf("%d ", root->key);
    inorder(root->right);
}
```
Здесь баланс определяется как:
height(left) - height(right)
Допустимые значения: −1, 0, 1.

# Балансировка узлов
В процессе добавления или удаления узлов в АВЛ-дереве возможно возникновение ситуации, когда balance factor некоторых узлов оказывается равными 2 или -2, т.е. возникает _расбалансировка_ поддерева. Для выправления ситуации применяются хорошо нам известные повороты вокруг тех или иных узлов дерева. Напомню, что простой поворот вправо (влево) производит следующую трансформацию дерева:
![[Pasted image 20260226155542.png]]
Код, реализующий правый поворот, выглядит следующим образом (как обычно, каждая функция, изменяющая дерево, возвращает новый корень полученного дерева):  
```C
node* rotateright(node* p) // правый поворот вокруг p
{
	node* q = p->left;
	p->left = q->right;
	q->right = p;
	fixheight(p);
	fixheight(q);
	return q;
}
```
Левый поворот является симметричной копией правого:  
```C
node* rotateleft(node* q) // левый поворот вокруг q
{
	node* p = q->right;
	q->right = p->left;
	p->left = q;
	fixheight(q);
	fixheight(p);
	return p;
}
```
Рассмотрим теперь ситуацию дисбаланса, когда высота правого поддерева узла p на 2 больше высоты левого поддерева (обратный случай является симметричным и реализуется аналогично). Пусть q — правый дочерний узел узла p, а s — левый дочерний узел узла q.
![[Pasted image 20260226155617.png]]
Анализ возможных случаев в рамках данной ситуации показывает, что для исправления расбалансировки в узле p достаточно выполнить либо простой поворот влево вокруг p, либо так называемый _большой поворот_ влево вокруг того же p. Простой поворот выполняется при условии, что высота левого поддерева узла q меньше высоты его правого поддерева: h(s)≤h(D).
![[Pasted image 20260226155722.png]]
Большой поворот применяется при условии h(s)>h(D) и сводится в данном случае к двум простым — сначала правый поворот вокруг q и затем левый вокруг p.
![[Pasted image 20260226155952.png]]


## Сбалансированные деревья в STL
STL напрямую использует **красно-чёрное дерево (Red-Black Tree)** в контейнерах:
- `std::set`
- `std::map`
- `std::multiset`
- `std::multimap`
Красно-чёрное дерево балансируется слабее AVL, но требует меньше вращений и лучше подходит для общего контейнера.
Пример:
```C++
#include <set>
#include <iostream>

int main()
{
    std::set<int> s;

    s.insert(10);
    s.insert(3);
    s.insert(7);
    s.insert(1);

    for (int v : s)
        std::cout << v << " ";
}
```
Гарантируется:
insert  -> O(log N)  
erase   -> O(log N)  
find    -> O(log N)
Порядок элементов всегда отсортирован, поскольку это BST.

## Почему STL выбрал Red-Black Tree, а не AVL
AVL обеспечивает меньшую высоту, но:
- требует больше вращений,
- дороже при частых вставках,
- сложнее обновлять.

Red-Black дерево допускает небольшую разбалансировку, зато уменьшает число перестроений. Для универсального контейнера это выгоднее.

Теги: #Прога 
Ссылки:
[[Дерево]]
[[Бинарное дерево поиска]]
[[Бинарное дерево]]
[[Контейнеры STL]]
[[Ассоциативный контейнер map С++]]